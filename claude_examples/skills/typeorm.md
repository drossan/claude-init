# Skill: TypeORM (typeorm)

## Propósito
Gestionar la capa de persistencia de datos de forma eficiente utilizando el ORM TypeORM.

## Responsabilidades
- Definir entidades mapeadas a tablas de base de datos.
- Configurar relaciones (OneToOne, OneToMany, ManyToMany) correctamente.
- Utilizar repositorios y el `QueryBuilder` para consultas optimizadas.
- Gestionar migraciones de base de datos para controlar cambios en el esquema.
- Asegurar el uso de transacciones cuando la integridad de los datos lo requiera.
- Implementar suscriptores o listeners si es necesario capturar eventos de base de datos.
